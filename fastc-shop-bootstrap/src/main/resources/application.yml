spring:
  application:
    name: fastc-shop-domain

springfox:
  documentation:
    swagger-ui:
      enabled: true

logging:
  config: classpath:logback.xml

fastbe:
  login:
    color:
      exclude: /swagger**/**,/webjars/**,/v3/**,/doc.html